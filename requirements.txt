# Full environment: generate.py needs the model stack.
# detect.py, by contrast, only needs requirements-detect.txt -- that split is
# the point: detection never needs torch, never touches model weights.

torch>=2.2
transformers>=4.40
numpy>=1.24
scipy>=1.11
pytest>=7.4
