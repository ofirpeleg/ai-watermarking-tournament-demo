<div dir="rtl">

# מדריך: סימון מים (Watermarking) בטקסט של מודלי שפה — גישת "טורניר"

מדריך זה מסביר את הפרויקט מהיסוד: הרעיון הכללי, למה יש שני תוכניות נפרדות,
מה כל קובץ עושה, המתמטיקה שמאחורי ההחלטה הסטטיסטית, ואיך להריץ ולנסות בעצמכם.

> **בחירת מודל:** ברירת המחדל של הפרויקט היא `Qwen/Qwen2.5-0.5B-Instruct` —
> מודל קל (כ-1GB, שניות בודדות לריצה על CPU), כדי שיהיה נוח ומהיר לנסות
> ולפתח איתו. ההיגיון של סימון המים לא תלוי בגודל המודל — אפשר להחליף
> ל-`Qwen/Qwen3-8B` (כ-8 מיליארד פרמטרים, כ-16GB הורדה, איטי משמעותית
> על CPU) או לכל מודל שפה אחר עם chat template דרך `transformers`, על ידי
> שינוי `MODEL_NAME` בקובץ `watermark_config.py` בלבד, בלי שינוי קוד נוסף.

## הרעיון הכללי

מודל שפה, בכל צעד של יצירת טוקן, כמעט תמיד יכול לבחור בין כמה טוקנים "טובים
באותה מידה" — לא רק את הטוקן היחיד בעל ההסתברות הגבוהה ביותר. סימון המים מנצל
את חופש הבחירה הזה: כשיש כמה מועמדים סבירים, אנחנו לא בוחרים את המועמד עם
ההסתברות הגבוהה ביותר סתם ככה — אנחנו בוחרים את זה שה"טביעת אצבע" הקריפטוגרפית
שלו (פונקציית גיבוב עם מפתח סודי) עונה על תנאי מסוים. התוצאה: טקסט שנראה טבעי
לגמרי לקורא אנושי, אבל מי שמחזיק את המפתח הסודי יכול לחשב, לכל טוקן, האם הוא
"נוטה" לכיוון שהמפתח קובע — ולצבור הטיה סטטיסטית קטנה על פני כל הטקסט.

הטיה קטנה כזו לא ניכרת בטוקן בודד, אבל על פני מאות טוקנים היא הופכת למובהקת
סטטיסטית (p-value נמוך מאוד) — וזה בדיוק מה שהגלאי (`detect.py`) מודד.

## למה שתי תוכניות נפרדות?

זו לא בחירה שרירותית — היא משקפת איך המערכת הזו עובדת במציאות:

- **`generate.py`** — רץ אצל מי ש*מפעיל את המודל*. הוא היחיד בפרויקט שטוען
  משקלים (`Qwen/Qwen2.5-0.5B-Instruct`), מייצר טקסט, וכותב קבצים.
- **`detect.py`** — רץ אצל מי שמקבל טקסט *אחר כך*, בלי גישה למודל ובלי לדעת
  מה היה הפרומפט — למשל מנחה קהילה, עיתונאי, או מבקר. **הוא אסור שיטען מודל
  בכלל.** כל מה שהוא צריך הוא הטוקנייזר (כדי להפוך טקסט בחזרה לרצף טוקנים)
  ו-`scipy` (לחישוב הסטטיסטיקה). לכן יש `requirements-detect.txt` נפרד,
  שבכוונה **לא כולל את torch בכלל** — כדי שההפרדה הזו תהיה בלתי ניתנת
  להחמצה.

ההפרדה הזו גם ממחישה תכונה מהותית: גלאי אמיתי בעולם האמיתי *אף פעם* לא רואה
את הפרומפט המקורי — הוא רואה רק את הטקסט שיצא. זו נקודה קריטית שחוזרת בהמשך
המדריך.

## ארכיטקטורת שני-התוכניות ומודול הליבה

```
watermark_core.py      <- ההיגיון המשותף. בלי torch, בלי transformers.
watermark_config.py    <- קריאת משתני סביבה, ברירות מחדל.
generate.py             <- טוען מודל, מייצר, כותב קבצים ל-runs/
detect.py               <- קורא קבצים, לעולם לא טוען מודל
tests/                  <- בדיקות מבוססות התפלגויות סינתטיות (בלי מודל)
```

הכלל המרכזי: **כל היגיון שגם היצירה וגם הגילוי חייבים להסכים עליו במדויק —
נמצא פעם אחת בלבד, ב-`watermark_core.py`.** זה אומר: פונקציית הגיבוב עם
המפתח, כלל "חלון ההקשר", שני השערים (אנטרופיה וסבירות), הטורניר, וחישוב
הסטטיסטיקה של הגילוי. כך היצירה והגילוי פיזית לא יכולים להתבדר זה מזה —
הבאג שמתואר בהמשך (סעיף "החלון חייב להכיל טוקנים שנוצרו בלבד") קרה בדיוק כי
ההיגיון הזה היה משוכפל בשני מקומות, לא משותף.

## האלגוריתם, שלב אחר שלב

לכל טוקן שנוצר:

```
logits -> softmax -> אנטרופיה של הצעד הזה
  |
  שער 1: אנטרופיה נמוכה מהסף?  -> המודל בטוח בעצמו: משאירים את הבחירה שלו, בלי סימון
  |
  top-K מועמדים לפי הסתברות
  |
  שער 2: משאירים רק מועמדים עם p >= min_ratio * p(המוביל)
          נשארו פחות מ-2?        -> אין בחירה אמיתית: משאירים את בחירת המודל
  |
  לכל מועמד ששרד: g_bits = m ביטים מגובבים עם מפתח
  |
  טורניר: הסכום הגבוה ביותר מנצח; תיקו נשבר לפי הסתברות גבוהה יותר
  |
  המנצח נפלט, מתווסף להקשר, וחוזר חלילה
```

**מודלים גדולים יותר מייצרים אנטרופיה נמוכה יותר, על אותו הפרומפט.** מודל
גדול יותר פשוט בטוח יותר בעצמו לגבי המילה הבאה — במיוחד במשימות נפוצות,
הוא ראה מספיק דוגמאות באימון כדי להיות די בטוח. נמדד על אותו פרומפט:
`Qwen2.5-0.5B-Instruct` נתן בממוצע כ-2.65 ביטים של אנטרופיה לטוקן,
`Qwen2.5-1.5B-Instruct` (פי 3 בגודל) ירד ל-כ-1.59, ומודל גדול בהרבה
(בסביבות 8B) ירד עוד יותר, לכ-0.67. פחות אנטרופיה אומר שפחות צעדים עוברים
את שער האנטרופיה, כלומר חלק קטן יותר מהטקסט בכלל מסומן — אז מודל גדול
יותר צריך יותר טוקנים כדי להגיע לאותה מובהקות סטטיסטית כמו מודל קטן.

### פונקציית הגיבוב עם המפתח

לכל שכבה `L` (0 עד `m-1`), הקשר `ctx`, וטוקן מועמד `t`:

```
g_bit(L, ctx, t) = הביט הנמוך של SHA256(key | L | ctx | t)
```

`ctx` הוא `context_window` הטוקנים ה**שנוצרו** האחרונים — **לעולם לא כולל את
הפרומפט**. הציון של מועמד הוא סכום ה-`m` הביטים שלו; הטורניר בוחר את המועמד
עם הסכום הגבוה ביותר, ושובר תיקו לפי הסתברות גבוהה יותר.

### דוגמה אמיתית מריצה

```
[   5] TOURNAMENT  H=4.393 bits
       candidates (top-8, m=256 bits each):
                ' beacon'  p=0.4017  bit_sum=119/256  g=0.465  [entered]
             ' prominent'  p=0.0801  bit_sum=118/256  g=0.461  [entered]
                  ' tall'  p=0.0568  bit_sum=123/256  g=0.480  [entered]
             ' structure'  p=0.0474  bit_sum=129/256  g=0.504  [entered]
              ' powerful'  p=0.0439  bit_sum=135/256  g=0.527  [entered]
                 ' large'  p=0.0278  [excluded: below min_ratio]
       matches:
         ' beacon'(bs=119) vs ' prominent'(bs=118) -> winner ' beacon' (higher bit-sum)
         ' tall'(bs=123) vs ' structure'(bs=129) -> winner ' structure' (higher bit-sum)
         ' beacon'(bs=119) vs ' structure'(bs=129) -> winner ' structure' (higher bit-sum)
         ' structure'(bs=129) vs ' powerful'(bs=135) -> winner ' powerful' (higher bit-sum)
       WINNER: ' powerful'  (g=0.527)
```

חמישה מועמדים היו קרובים מספיק בהסתברות כדי להיחשב אפשרויות אמיתיות; הטורניר
בחר מביניהם — ולא בחר את המועמד המועדף על המודל (`' beacon'`), אלא ב-`'
powerful'`, טוקן שהמודל עדיין אהב (p=0.044, בתוך הרף של `min_ratio`). זה
בדיוק המנגנון פועל כמתוכנן: דחיפה בין אפשרויות סבירות באמת — לא החלפה בטוקן
אבסורדי.

### שער האנטרופיה לבדו לא מספיק

צעד אמיתי מגרסה מוקדמת יותר של הפרויקט, *לפני* ששער הסבירות (`min_ratio`)
היה קיים:

```
' Name'   p=0.9950
' Names'  p=0.0018
```

שני אלה הם "top-K" — אבל המודל היה בטוח ב-99.5%. אם נותנים לטורניר לבחור
חופשי בין השניים, הוא לפעמים יבחר ב-`' Names'` — טוקן פחות סביר פי 550 —
ותריסר בחירות כאלה ברצף הפיקו טקסט כמו *"DNS was developed by Ray Tomsho in
1891"*. שער האנטרופיה לבדו לא עוצר את זה: לצעד יכולה להיות אנטרופיה מספקת
כדי לעבור שער 1, ועדיין יש מועמד אחד עם p=0.995 וכל השאר קרוב לאפס.
`min_ratio` (ברירת מחדל 0.3) הוא זה שמונע את זה — `' Names'` ב-p=0.0018 רחוק
מאוד מ-30% מ-p=0.995 של `' Name'`, ולכן הוא מוחרג עוד לפני שהטורניר מתחיל.

זו גם הסיבה שרוב הצעדים **לא** מסומנים בכלל: ב-`entropy_min=2.0` ו-
`min_ratio=0.3` (ברירות מחדל מחמירות בכוונה, ראו הסבר בהמשך), חלק גדול
מהצעדים יש להם או אנטרופיה נמוכה או מועמד יחיד דומיננטי, ושני השערים
"נופלים דרך" לבחירה הרגילה (greedy) של המודל, בלי שום
הטיה. סימון מים קורה רק בצעדים שבהם למודל היו באמת כמה אפשרויות טובות.

### ההקשר חייב להכיל טוקנים שנוצרו בלבד — לא את הפרומפט

זה היה באג אמיתי בפיתוח: היצירה גיבבה כל מועמד עם *כל* הרצף עד כה — כולל
כ-42 טוקני הפרומפט — בעוד הגילוי, שרואה רק את הטקסט שיצא, התחיל מהקשר ריק.
קלטים שונים לגיבוב בשני הצדדים פירושם ערכי g לא קשורים בכלל. התוצאה: "לא
זוהה סימן מים" בביטחון מלא, על טקסט שבו *כל* טוקן נבחר בפועל על ידי הטורניר
בגלל סכום ביטים גבוה.

גלאי אף פעם לא מקבל את הפרומפט — רק את הטקסט. לכן כל דבר שהפרומפט נוגע בו
פשוט לא ניתן לשחזור בצד הגילוי. הפתרון: חלון חסום (ברירת מחדל: 4) על פני
**טוקנים שנוצרו בלבד**, ממומש פעם אחת כ-`watermark_core.context_window`
ונקרא באופן זהה משני התוכניות. חלון חסום גם מגביל את "רדיוס הפגיעה" — אם מישהו
עורך טוקן אחד באמצע טקסט מסומן, רק ה-`context_window` הטוקנים הבאים
מושפעים, לא כל מה שאחריהם.

הבדיקה `tests/test_watermark.py::test_alignment_detector_recovers_exact_generation_time_g`
היא בדיקת הרגרסיה לבאג הזה: היא מייצרת רצף מסומן סינתטי, ואז מחשבת מחדש,
*באופן בלתי תלוי*, את ציון ה-g של כל טוקן מתוך רצף הפלט בלבד — ובודקת
שהתוצאות זהות לחלוטין.

### הגלאי חייב לספור כל זוג (הקשר, טוקן) פעם אחת בלבד

מבחן ה-z מניח שכל זוג שנספר הוא הטלת מטבע בלתי-תלויה. אם המודל נתקע בלולאה
על טוקן אחד, אותו זוג (הקשר, טוקן) חוזר על עצמו — אותו גיבוב, אותם ביטים,
נספרים שוב ושוב, וזו לא עדות בלתי-תלויה. בלי טיפול, 300 חזרות של טוקן
שמעולם לא סומן יכולות להפיק Mean G = 0.9933, p = 9e-66 — זיהוי שקרי ומובהק
לחלוטין על טקסט שמעולם לא סומן. `detect.py` מבצע הסרת כפילויות כברירת מחדל;
`--no-dedup` מכבה את זה בכוונה, כדי להדגים את כשל.

### Mean G אף פעם לא קרוב ל-1

בצעד שבאמת סומן, המנצח מקבל g=0 (כל ה-`m` הביטים אפס) רק במקרה הלא-סביר
שבו *כל* המועמדים קיבלו זאת — כך שגם בצעדים מסומנים, הציפייה היא בערך
`1 - 2^-K`, לא 1. ועל פני טקסט שלם, זה מדולל עוד יותר על ידי כל צעד שדולג
עליו, שתורם g בלתי-מוטה סביב 0.5:

```
Mean G  =  0.5 + f * (הטיה לביט בצעדים המסומנים)      כאשר f = חלק הצעדים שסומנו
```

**Mean G קרוב ל-1.0 הוא לא סימן מים חזק — הוא סימן מים ללא שערים.** מערכת
אמיתית ששוערת לפי אנטרופיה וסבירות בהכרח משאירה את רוב הטקסט ללא נגיעה,
כך שה-Mean G שלה נשאר קרוב ל-0.5, וההסטה נישאת על ידי העוצמה הסטטיסטית
(`N * m`), לא על ידי דחיפת כל טוקן לקיצון.

### דוגמה זעירה: מאיפה `g=1 count` / `g=0 count` הגדולים האלה מגיעים

ב-`detect.py` רואים שדות כמו `g=1 count: 28462` — מספרים גדולים בהרבה
מ-`Tokens analyzed`. הסיבה: השדות האלה סופרים **ביטים בודדים**, לא טוקנים.
כל טוקן מקבל `rounds` (ברירת מחדל 30) ביטים נפרדים, אחד לכל "סיבוב"
בפונקציית הגיבוב. נניח, לצורך הדוגמה, שהיו רק 4 סיבובים במקום 30, ושלושה
טוקנים נבדקו:

| טוקן | סיבוב 0 | סיבוב 1 | סיבוב 2 | סיבוב 3 | סכום 1-ים |
|---|---|---|---|---|---|
| א' | 1 | 0 | 1 | 1 | 3 |
| ב' | 0 | 0 | 1 | 0 | 1 |
| ג' | 1 | 1 | 1 | 0 | 3 |

סה"כ תאים בטבלה = 3 טוקנים × 4 סיבובים = 12. `g=1 count` = 3+1+3 = 7,
`g=0 count` = 12-7 = 5, ו-Mean g-score = 7/12 = 0.583. בריצה אמיתית זה
בדיוק אותו חישוב, רק עם 30 סיבובים במקום 4 ומאות טוקנים במקום 3 — הטבלה
גדולה יותר, אבל האריתמטיקה זהה. `Tokens analyzed` סופר **שורות** בטבלה;
`g=1`/`g=0 count` סופרים **תאים**.

### כיול: מה `m` (מספר השכבות) באמת קונה לכם

סימולציה של 20,000 ניסויים, 5 מועמדים ברי-קיימא בטורניר, חלק שוערים f=0.45,
N=700 טוקנים:

| שכבות m | Mean G על הטקסט | z | p | בוחר בטוקן המועדף של המודל |
|---:|---:|---:|---:|---:|
| 1 | 0.711 | 11.16 | 3e-29 | 53.3% |
| 4 | 0.625 | 13.24 | 3e-40 | 34.2% |
| 30 | 0.548 | 13.82 | 1e-43 | 24.1% |
| 100 | 0.526 | 13.90 | 3e-44 | 22.4% |
| 300 | 0.515 | 13.87 | 5e-44 | 21.8% |
| 680 | 0.510 | 13.83 | 9e-44 | 20.5% |

שני דברים עולים מהטבלה:

- **ה-z כמעט לא זז.** יותר שכבות לא יוצרות יותר עדות — הן פורשות את אותה
  עדות על פני יותר ביטים, כך שההטיה לביט מתכווצת בדיוק באותו קצב שבו מספר
  הביטים גדל. העוצמה הסטטיסטית מגיעה מ-`f` ו-`N`, לא מ-`m`. מה ש-`m` באמת
  קונה הוא Mean G שנראה כמו מערכת production, ולא כמו כלי כוח-גס.
- **שכבות עולות באיכות טקסט.** ב-`m=1`, תיקו בסכום הביטים שכיח, ואז ההסתברות
  של המודל שוברת את התיקו — כך שהבחירה המועדפת על המודל שורדת 53% מהזמן.
  ב-`m=680`, תיקו כמעט לעולם לא קורה, המנצח נבחר בפועל כמעט אקראית בין
  המועמדים ברי-הקיימא (20% ≈ סיכוי טהור עם 5 כאלה), והעדפת המודל כבר לא
  משנה כלל. `m` לא "בחינם" — העלאתו מוכרת שטף (fluency) תמורת Mean G "נקי"
  יותר.

ברירת המחדל כאן (`WATERMARK_ROUNDS=30`) תואמת את מספר הסבבים שבו משתמשת
סכימת הטורניר המקורית שהפרויקט הזה מבוסס עליה (ראו את קטע הקרדיטים למאמר
ולסרטון למטה): Mean G סביב 0.55, הרחק מתחת לסף `p < 0.01`, תוך כדי כיבוד
העדפות המודל בערך פעם אחת מכל 4. כיוון ש-z כמעט לא זז לאורך הטווח הזה, 30
זו לא פשרה על עוצמה סטטיסטית — זו פשוט נקודת ההתייחסות שרצינו להתאים אליה.
העלאת `m` (100–300) נותנת Mean G "נקי" עוד יותר במחיר של כיבוד פחות תדיר את
העדפת המודל; לחלופין, שער אנטרופיה הדוק יותר (`f ≈ 0.10`) מגיע ל-Mean G ≈
0.511 ב-p ≈ 7e-4 — קרוב יותר למה שמספרים של מערכת production נראים — במחיר
של צורך בטקסט ארוך יותר כדי להגיע לאותה מובהקות, כי העוצמה משתנה לפי `f * N`.

## הליכה קובץ-אחר-קובץ, פונקציה-אחר-פונקציה

### `watermark_core.py`

- `keyed_bit(key, round_num, context, token_id)` — הפרימיטיב היחיד שהכל בנוי
  עליו: ביט אחד, קבוע, מ-SHA256.
- `context_window(generated_tokens, window)` — הכלל המשותף. "הטוקנים
  שנוצרו בלבד, לעולם לא הפרומפט". נקרא בדיוק אותו דבר משני הצדדים.
- `bit_sum` / `g_score` — סכום/ממוצע של `m` הביטים לזוג (הקשר, טוקן) נתון.
- `Candidate`, `Match`, `StepResult` — מבני נתונים לדיווח.
- `run_tournament(viable)` — הטורניר בפועל: בראקט בשלה-אחת, זרוע לפי
  הסתברות. מכיוון ש"סכום ביטים גבוה יותר מנצח" היא השוואה טרנזיטיבית,
  המקסימום האמיתי תמיד שורד לסיבוב האחרון, ללא תלות בזריעה.
- `decide_step(...)` — הפונקציה המרכזית: מקבלת top-K מוכן, אנטרופיה,
  ורצף הטוקנים שנוצרו עד כה, ומחזירה `StepResult` שלם. פונקציה טהורה —
  בלי מודל, בלי I/O — ולכן ניתנת לבדיקה עם התפלגויות מזויפות.
- `sequence_to_pairs(token_ids, window)` — הופך רצף טוקנים שטוח לרשימת
  זוגות (הקשר, טוקן), בשימוש הן ב-`generate.py` (לצורך הדוח) והן
  ב-`detect.py`.
- `deduplicate_pairs` — הסרת כפילויות של זוגות.
- `compute_detection(pairs, key, rounds, p_threshold, dedup)` — מחשב את
  הסטטיסטיקה כולה: ספירות g=1/g=0, Mean G, sigma, z, p-value, ופסיקה.

### `watermark_config.py`

קריאת משתני הסביבה, עם ברירות מחדל, ובלי תלות ב-torch/transformers.
`WATERMARK_MAX_TOKENS` (ברירת מחדל 700) הוא רק **תקרה** — לא יעד. המודל
עוצר לבד מתי שהוא רוצה; שום דבר בקוד לא מכריח אותו להמשיך.

### `generate.py`

- `parse_args` — מפרסר את `--prompt TEXT` (**חובה** — אין יותר קריאה
  אינטראקטיבית, הכל דרך שורת הפקודה), `--watermarked`/`--watermark`
  (בלעדיו: טקסט רגיל בלבד), `--model NAME` (מודל אחר לריצה הזו),
  `--max-tokens N` (במקום לגזור מ-`WATERMARK_MAX_TOKENS`), ו-`--outputfile NAME`
  (שם קובץ מותאם אישית במקום חותמת זמן — למשל `--outputfile withwatermark`
  כותב `runs/withwatermark.txt`).
- `pick_device` — בוחר `cuda`/`mps`/`cpu` אוטומטית, כדי לנצל GPU של Mac
  (Apple Silicon) בלי צורך בדגל.
- `validate_prompt` — דוחה פרומפט ריק או ארוך מדי (מעל 500 מילים).
- `build_prompt_ids` — עוטף את הפרומפט בתבנית הצ'אט (chat template) של
  המודל. אם עוברים למודל ממשפחת Qwen3, שיש לה מצב "חשיבה", מוסיף
  `enable_thinking=False` כדי לקבל תשובה ישירה בלי בלוק `<think>...</think>`
  (ומתעלם מהפרמטר בשקט אם התבנית לא תומכת בו — למשל אצל Qwen2.5).
- `generate_normal` — יצירה רגילה, greedy, ללא סימון (קבוצת ביקורת). עוצרת
  לבד ברגע שהמודל בוחר בטוקן הסיום, או מגיעה לתקרת `max_new_tokens`. רצה רק
  כש-`--watermarked` לא הועבר.
- `generate_watermarked` — לולאה ידנית, טוקן-אחרי-טוקן, עם מטמון
  key/value (`past_key_values`) ליעילות: בכל צעד מחשבת logits, סופטמקס,
  אנטרופיה, top-K, וקוראת ל-`decide_step`. עוצרת באותו אופן טבעי כמו
  `generate_normal`. רצה רק כש-`--watermarked` כן הועבר.
- `build_normal_report` / `build_watermarked_report` — שני בוני-דוח נפרדים,
  אחד לכל מצב: הדוח של הטקסט הרגיל קצר (אין טורניר לתאר), הדוח של הטקסט
  המסומן כולל את הסיכום המלא ואת פירוט הטורנירים לכל טוקן.
- `main` — טוען את המודל פעם אחת, אבל מריץ **יצירה אחת בלבד** לפי הדגל,
  כותב שני קבצים ל-`runs/` (טקסט + דוח) עם חותמת זמן בשם (או השם שנתתם
  ב-`--outputfile`), ומדפיס סיכום קצר. במצב `--watermarked` הסיכום כולל שורת
  "Watermarking Summary" (כמה צעדים עברו טורניר, כמה מהם היו עם יותר
  ממועמד אחד, ואנטרופיית טוקן ממוצעת). כדי לקבל השוואה מלאה, מריצים פעמיים
  (עם אותו פרומפט, פעם עם `--watermarked` ופעם בלי) ומעבירים את שני קובצי
  ה-`.txt` יחד ל-`detect.py`.

### `detect.py`

- `format_p` — מציג p-value תמיד כמספר עשרוני, **בלי כתיב מדעי (`e`)**,
  עם בדיוק מספיק ספרות כדי שהערך באמת ייראה (`0.0001244` נשאר גלוי, לא
  מתעגל ל-`0.0000000`) — עד תקרה של 20 ספרות. מעבר לזה, float64 עצמו כבר
  לא יכול להבדיל בין הערך לבין אפס, אז יותר אפסים לא באמת מוסיפים מידע.
- `LEGEND` — טקסט קבוע שמסביר בקצרה כל שדה בפלט; מודפס פעם אחת בתחילת
  הריצה (`--no-legend` מדלג עליו).
- `load_docs_from_file` — `generate.py` כותב היום רק `.txt` (בלי JSON):
  הקובץ מטוקן מחדש עם הטוקנייזר בלבד, לפי `--model` או ברירת המחדל —
  בלי שום סיכום טורנירים מוטבע, כי טקסט גולמי לא נושא את המידע הזה. הפונקציה
  עדיין תומכת גם בקריאת קובץ `.json` (אם למישהו יש כזה מריצה ישנה, או בונה
  אחד ידנית) — אז אם יש `gen_summary` (סיכום הטורנירים) הוא יוצג; אם לא,
  פשוט מדלגים על השורה הזו.
- `print_doc_report` / `print_comparison` — פלט לטרמינל, מלא כברירת מחדל:
  Tokenizer, Key/Context window/Rounds/Dedup, Tokens analyzed, g=1/g=0,
  Mean g-score, z-score, p-value, ושורת "Tournament summary" כשיש
  `gen_summary`. **בכוונה בלי שורת Sigma** — היא ערך ביניים, לא משהו
  שפועלים לפיו ישירות; z-score ו-p-value כבר נושאים את כל מה שהיא מזינה.
  כותרת "File:" מוצגת רק כשיש יותר מקובץ אחד.
- `main` — פרסור CLI (כולל `--input` כחלופה/תוספת לקבצים הפוזיציונליים,
  `--model` לדריסת הטוקנייזר לקבצי `.txt`, `--key`, `--window`, `--rounds`,
  `--p-threshold`, `--no-dedup`, `--no-legend`), הרצת גילוי על כל מסמך, והדפסה.

### `tests/synthetic.py` ו-`tests/test_watermark.py`

מייצרים התפלגויות הסתברות מלאכותיות (בלי מודל בכלל) ומריצים אותן דרך
`decide_step` האמיתי — כך שהבדיקות בודקות את ההיגיון עצמו, מהר ובאופן
דטרמיניסטי, בלי צורך ב-GPU או רשת.

## התקנה והרצה

```bash
python3 -m venv .venv
source .venv/bin/activate        # Windows: .venv\Scripts\activate
pip install -r requirements.txt  # הסביבה המלאה, כולל torch, ל-generate.py
```

אם רוצים להריץ רק את `detect.py` (למשל על מכונה שלעולם לא תיגע במודל):

```bash
pip install -r requirements-detect.txt
```

הרצה:

```bash
python generate.py --prompt "כתוב שתי משפטים על מגדלורים." # טקסט רגיל בלבד
# יכתוב 2 קבצים ל-runs/ (טקסט + דוח), עם חותמת זמן בשם (או --outputfile NAME)

python generate.py --prompt "כתוב שתי משפטים על מגדלורים." --watermarked  # אותו פרומפט, עם סימון מים
# שוב 2 קבצים, כולל דוח מלא עם פירוט הטורנירים

python detect.py runs/run_<ts1>_normal.txt runs/run_<ts2>_watermarked.txt
# לג'נדה פעם אחת, ואז לכל קובץ: Tokenizer, Key/Rounds/Dedup, Tokens analyzed,
# g=1/g=0, Mean g-score, z-score, p-value (בלי Sigma) — ואז טבלת השוואה
```

כל ריצה של `generate.py` מפיקה **טקסט אחד בלבד** — היצירה דטרמיניסטית
(greedy, בלי sampling), אז אותו פרומפט מול אותו מודל תמיד ייתן את אותו
טקסט בכל צד, גם אם הריצות התבצעו בזמנים שונים. כדי לקבל השוואה מלאה,
מעבירים את שני קובצי ה-`.txt` יחד ל-`detect.py`, כמו למעלה.

שימו לב: המודל תמיד עוצר לבד, מתי שהוא רוצה — `max_new_tokens`/`--max-tokens`
הוא רק תקרה בטיחותית, לא יעד. **בכוונה אין אפשרות בפרויקט הזה להכריח את
המודל להמשיך מעבר לנקודת העצירה הטבעית שלו** — ניסינו את זה בעבר (מנגנון
שנקרא `WATERMARK_MIN_FRACTION`) וגילינו שזה גורם למודלים קטנים להיתקע
בלולאת חזרות (אותה משפט שוב ושוב), וה-dedup אז (בצדק) זורק את רוב זה —
כך שנשארים עם **פחות** טוקנים שימושיים מאשר עצירה טבעית, לא יותר. אם רוצים
טקסט ארוך יותר, הדרך הבטוחה היא לבקש את זה בפרומפט עצמו (למשל "כתוב חיבור
של 2000 מילים") ולתת למודל לבחור לכתוב יותר בעצמו.

להריץ את הבדיקות:

```bash
pytest tests/ -v
```

## ניסויים כדאיים לנסות

1. **המפתח הלא נכון:** `python detect.py runs/run_<ts>_watermarked.txt --key wrong-key` —
   Mean G קורס בחזרה לכיוון 0.5, אין זיהוי.
2. **חלון ההקשר הלא נכון:** אותו דבר עם `--window` שונה מהערך שבו נוצר
   הטקסט.
3. **הדגמת false positive מחזרתיות:** קחו טקסט עם טוקן אחד שחוזר עשרות
   פעמים, והריצו עם `--no-dedup` מול ברירת המחדל (עם dedup) — ראו את ההבדל
   העצום ב-z וב-p.
4. **שינוי `WATERMARK_ROUNDS`:** נסו `WATERMARK_ROUNDS=1` מול
   `WATERMARK_ROUNDS=680` ותצפו איך Mean G, ואיכות הטקסט (עד כמה הוא נשאר
   קרוב לבחירת המודל המקורית), משתנים — בהתאם לטבלת הכיול למעלה.
5. **שינוי `WATERMARK_ENTROPY_MIN` ו-`WATERMARK_MIN_RATIO`:** שערים הדוקים
   יותר מורידים את `f` (חלק הצעדים המסומנים) — Mean G קרוב יותר ל-0.5,
   אבל צריך טקסט ארוך יותר כדי להגיע לאותה מובהקות. זו בדיוק הסיבה
   שברירת המחדל היום היא `2.0`/`0.3` ולא `0.5`/`0.1`: עם השערים המקוריים
   (רפויים יותר) קיבלנו z=11.5 ו-p שהוא קיר של 20 אפסים — נכון, אבל לא
   קריא. עם השערים המחמירים יותר (ברירת המחדל הנוכחית) אותו פרומפט נתן
   z=5.03, p=0.0000002458 — עדיין מובהק בבירור, אבל מספר שאפשר בפועל
   לקרוא. אם תרצו לחזור למספרים "קיצוניים" יותר, `WATERMARK_ENTROPY_MIN=0.5
   WATERMARK_MIN_RATIO=0.1` מחזיר את ההתנהגות הישנה.
6. **עריכת הטקסט המסומן:** שנו מילה אחת בתוך `run_<ts>_watermarked.txt`
   ובדקו כמה מהזיהוי נשאר — ותצפו שההשפעה מוגבלת בערך ל-`context_window`
   הטוקנים הבאים בלבד, לא לכל הטקסט שאחרי העריכה.

## גבולות הפרויקט, והמאמר שמאחורי הרעיון

זהו יישום חינוכי, שנועד להנגיש את המנגנון — לא מערכת production. הוא לא
[SynthID-Text](https://www.nature.com/articles/s41586-024-08025-4)
(Dathathri ואחרים, *Nature* 634, 818–823, 2024) או LUNA, הוא לא עמיד בפני
פרפרזה, תרגום, או עריכה משמעותית (ראו את "Scope and limitations" ב-README.md
האנגלי לפרטים המלאים). SynthID-Text הוא המאמר האמיתי שהפרויקט הזה שואב ממנו
השראה — הוא פרוס בפועל בצ'אטבוטים של Gemini בגוגל, וכדאי לקרוא אותו ישירות
כדי לראות איך מערכת production אמיתית מטפלת בעיוות הדגימה (sampling
distortion), בדגימת טורניר, ובגילוי בקנה מידה גדול.

כמה מברירות המחדל הקונקרטיות כאן (`WATERMARK_TOP_K=20`, `WATERMARK_CONTEXT=4`,
`WATERMARK_ROUNDS=30`) נבחרו כדי להתאים למימוש-מאפס של דגימת טורניר שמוסבר
ומודגם בסרטון
[Computerphile — "How Watermarks Track AI Generated Content"](https://www.youtube.com/watch?v=kVXp6UNVPTo),
שמסביר ומדגים את אותו מנגנון G-score/טורניר שהפרויקט הזה מממש.

</div>
